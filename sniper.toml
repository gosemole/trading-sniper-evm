# Enough config to start --watch-launches, and nothing more. The pool is a
# placeholder: this command never resolves one. Do NOT run the bot itself on
# this file.
threshold_pct = 3

[[pools]]
name = "placeholder"
version = "v3"
address = "0x0000000000000000000000000000000000000001"

[snipe]
size_x100           = 300
# size              = "0.5"   # потолок в валюте пары, если нужен
slippage_bps        = 100
max_tax_bps         = 19
operators      = "operators.json"
max_creator_tax_bps = 300
max_run_x100        = 300
max_exempt          = 50
lead_ms             = 100
contract = "0x75C1243Da9AF8Bd6b01193de40A8dc4Af75C6cb2"
require_dev_buy     = true
min_dev_buy_x100    = 50
